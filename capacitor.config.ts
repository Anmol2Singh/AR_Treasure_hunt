import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.anmol.artreasurehunt',
  appName: 'AR Treasure Hunt',
  webDir: 'dist'
};

export default config;
